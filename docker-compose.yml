services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: dommecraft
      POSTGRES_PASSWORD: dommecraft_dev
      POSTGRES_DB: dommecraft
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
